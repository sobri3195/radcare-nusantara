import { Link } from 'react-router-dom';
import SectionHeader from '../ui/SectionHeader';
export default function TodaySchedule({ items, patientMap }: any) {return <section className='rounded-2xl border border-slate-200 bg-white p-4 shadow-sm'><SectionHeader title='Jadwal Terapi Hari Ini' /><div className='space-y-2'>{items.map((s:any)=><div key={s.id} className='flex items-center justify-between rounded-xl border border-slate-100 p-3'><div><p className='text-sm font-semibold'>{s.time || '-' } • {patientMap[s.patientId]?.name || 'Pasien'}</p><p className='text-xs text-slate-500'>{s.room || s.machine || 'Ruang LINAC'} • {s.status}</p></div><Link to='/schedule' className='min-h-11 rounded-lg bg-slate-100 px-3 py-2 text-xs font-medium'>Detail</Link></div>)}</div></section>}
