import { useState } from 'react';import { generateInsight } from '../lib/aiRules';
const prompts=['Pasien mana yang perlu evaluasi?','Bagaimana ringkasan jadwal hari ini?','Apa risiko tertinggi minggu ini?','Ringkas progress terapi pasien aktif.','Buat rekomendasi operasional hari ini.'];
export default function AIAssistantPage(){const [q,setQ]=useState(''); const [a,setA]=useState(''); return <div className='space-y-3'><h2 className='text-2xl font-bold'>AI Assistant RADCARE</h2><p>Asisten simulasi untuk membantu membaca pola monitoring, jadwal, dan risiko pasien.</p><div className='rounded-2xl bg-white p-4 shadow'><div className='mb-2 text-sm text-rose-600'>AI Assistant hanya untuk simulasi dan tidak menggantikan keputusan klinis.</div><div className='flex flex-wrap gap-2'>{prompts.map(p=><button key={p} className='rounded-full bg-slate-100 px-3 py-1 text-sm' onClick={()=>setQ(p)}>{p}</button>)}</div><textarea value={q} onChange={e=>setQ(e.target.value)} className='mt-3 w-full rounded-xl border p-3'/><button className='mt-2 rounded-xl bg-cyan-600 px-4 py-2 text-white' onClick={()=>setA(generateInsight(q))}>Generate Insight</button>{a&&<div className='mt-3 rounded-xl bg-cyan-50 p-3'>{a}</div>}</div></div>}
